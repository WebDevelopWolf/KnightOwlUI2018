import { KeeperlogfilterPipe } from './keeperlogfilter.pipe';

describe('KeeperlogfilterPipe', () => {
  it('create an instance', () => {
    const pipe = new KeeperlogfilterPipe();
    expect(pipe).toBeTruthy();
  });
});
