import { VetlogfilterPipe } from './vetlogfilter.pipe';

describe('VetlogfilterPipe', () => {
  it('create an instance', () => {
    const pipe = new VetlogfilterPipe();
    expect(pipe).toBeTruthy();
  });
});
